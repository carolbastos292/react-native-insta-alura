import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  viewComment: {
    flexDirection: 'row',
    alignItems: 'center',
    marginLeft: 10,
  },
  imgSend: {
    width: 20,
    height: 20,
    marginRight: 10,
  },
});

export default styles;
