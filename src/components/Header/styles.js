import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  imageUser: {
    width: 30,
    height: 30,
    margin: 10,
    borderRadius: 30,
  },
  header: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});

export default styles;
